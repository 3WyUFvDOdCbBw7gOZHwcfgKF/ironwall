{program test~std~sys~fdatasync@main
  (import std~windows~sys)
  (import test~std~sys~lib)
  (function main ([args <array s3>]) to i5 in
    {
      (var [fd i5] (sys_fd_open_write_s3 $fdatasync_path_s3^s3))
      (var [written i5] (sys_fd_write_s3 fd $fdatasync_payload_s3^s3))
      (sys_fd_fdatasync fd)
      (sys_fd_close fd)
      (add
        $270^i5
        (add
          (score (gt fd $2^i5))
          (score (eq written (s3_length $fdatasync_payload_s3^s3)))
        )
      )
    }
  )
}
