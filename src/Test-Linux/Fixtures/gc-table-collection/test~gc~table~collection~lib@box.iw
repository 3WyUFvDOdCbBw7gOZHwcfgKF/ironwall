{program test~gc~table~collection~lib@box
  (var [lib_text s3] (s3_new $3^i5 (s3_get $hello_s3^s3 $0^i5)))
  (var [lib_rank i5] $2^i5)
  (class LibBox
    (property [name s3])
    (constructor ([name0 s3]) in (cm_set self name name0))
  )
  (function make_lib_box () to LibBox in (class_new LibBox lib_text))
  (function lib_score () to i5 in (add (s3_length lib_text) lib_rank))
}
