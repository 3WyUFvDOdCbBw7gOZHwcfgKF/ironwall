{program test~x64~support_c@main
  (import std~ord)
  (import std~math)
  (function compare_i5 ([left i5] [right i5]) to i5 in
    (if (lt left right) then $0neg1^i5 else (if (gt left right) then $1^i5 else $0^i5))
  )
  (function main ([args <array s3>]) to i5 in
    {
      (var [rule <Ord i5>] (class_new <Ord i5> compare_i5))
      (pi_f6)
      (tau_f7)
      (<ord_compare i5> rule $7^i5 $5^i5)
    }
  )
}
